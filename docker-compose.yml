version: "3.7"
services:
    venapp-service:
       container_name: venapp-landing
       build: .
    nginx-service:
       container_name: nginx
       build: ./docker-files
       ports:
         - "443:443"
